# Absolute path to your local vault directory (the folder containing captures/, notes/, etc.)
# This directory will be created if it doesn't exist.
VAULT_LOCAL_PATH=/Users/yourname/path/to/second-brain
